import Agent from './agent';
import { AgentOps } from './declarations/agent';
declare const AgentManager: {
    getAgent(): Agent;
    initializeAgent(ops: AgentOps): Agent;
    /**
     * Destroy and remove the event listeners associated with the listeners
     */
    destroyAgent(): void;
};
export default AgentManager;
