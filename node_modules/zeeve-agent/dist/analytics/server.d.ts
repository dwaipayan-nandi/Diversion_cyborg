import { AnalyticsOps } from '../declarations/analytics';
export default class AnalyticsServer {
    private analyticsPort;
    private analyticsHost;
    private wsClient;
    private server?;
    constructor(options: AnalyticsOps);
    /**
     * Start listening for HTTP requests for analytics data from
     * Prometheus remote_write service
     */
    startListening(): void;
    /**
     * remove the HTTP service event listener
     */
    destroyServer(): void;
}
