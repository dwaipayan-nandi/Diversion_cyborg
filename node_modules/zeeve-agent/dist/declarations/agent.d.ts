import { ClientCallback } from '../wsclient';
import { executeScriptCallbacks } from './actions';
export declare type AgentOps = {
    serverAddress?: string;
    clientCallbacks?: ClientCallback;
    executeScriptCallbacks?: executeScriptCallbacks;
    limitFrequencyBasedExecution?: {
        howmany: {
            times: number;
            callback: () => void;
        };
    };
    serverAddressUpdater?: () => Promise<string>;
    enableAnalytics?: boolean;
    analyticsPort?: number;
    analyticsHost?: string;
};
export declare const AUTOMATIC_RECONNECTION_WAITING_TIME = 10000;
