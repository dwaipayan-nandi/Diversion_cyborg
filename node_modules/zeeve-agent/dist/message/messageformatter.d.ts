import { executeScriptCallbacks } from '../declarations/actions';
import { MessageToServer, OutputRequestObject } from '../declarations/message';
export default class MessageFormatter {
    static prepareLogMessage(task_id: string, log: string): MessageToServer;
    static prepareScriptExecutionUpdateMessage(task_id: string, success: boolean, messageOrCode: string | number | null, outputRequestObjects?: OutputRequestObject[]): MessageToServer;
    static parseAndExecute(msg: any, exCallbacks?: executeScriptCallbacks): void;
}
