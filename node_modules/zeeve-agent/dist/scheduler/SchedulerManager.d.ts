import { schedulerOps } from '../declarations/scheduler';
import Scheduler from './Scheduler';
declare const SchedulerManager: {
    getScheduler(): Scheduler;
    initializeScheduler(ops: schedulerOps): Scheduler;
    destroyScheduler(): void;
};
export default SchedulerManager;
