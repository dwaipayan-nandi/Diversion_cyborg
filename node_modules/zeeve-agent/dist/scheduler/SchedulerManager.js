"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var Scheduler_1 = __importDefault(require("./Scheduler"));
var SchedulerManager = (function () {
    var scheduler;
    var defaultOps = {};
    function createscheduler(ops) {
        var s = new Scheduler_1.default(ops);
        return s;
    }
    return {
        getScheduler: function () {
            if (!scheduler) {
                scheduler = this.initializeScheduler(defaultOps);
            }
            return scheduler;
        },
        initializeScheduler: function (ops) {
            if (!scheduler) {
                scheduler = createscheduler(ops);
            }
            return scheduler;
        },
        destroyScheduler: function () {
            this.getScheduler().clearTasks();
            scheduler = null;
        },
    };
})();
exports.default = SchedulerManager;
